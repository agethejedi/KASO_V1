{
  "name": "fraud-response-voice-agent-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2025-01-15",
    "deploy": "wrangler pages deploy public",
    "check": "node -e \"console.log('No build step required.')\""
  },
  "devDependencies": {
    "wrangler": "^4.37.1"
  }
}
